import styled from "styled-components";

import { H6HeadingTitle } from "@/globalStyle";

const Wrapper = styled.div`
  display: inline-block;
  border-radius: 8px;
  background-color: var(--color-text-primary-700);
  ${H6HeadingTitle};
  color: #fff;
  padding: 8px 12px;
`;

export { Wrapper };
